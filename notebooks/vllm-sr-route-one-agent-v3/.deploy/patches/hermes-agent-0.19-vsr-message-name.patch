--- a/agent/tool_dispatch_helpers.py
+++ b/agent/tool_dispatch_helpers.py
@@ -464,3 +464,7 @@
-    """Build a tool-result message dict with both the OpenAI-format ``name``
-    field (required by the wire format and provider adapters) and the internal
-    ``tool_name`` field (written to the session DB messages table).
+    """Build a tool-result message with the internal ``tool_name`` field.
+
+    The optional OpenAI ``name`` field is omitted from the wire-facing message
+    because vLLM Semantic Router's protocol-neutral request contract rejects
+    ``messages[].name``. ``tool_call_id`` carries the required call identity,
+    while ``tool_name`` remains available for Hermes session indexing and is
+    stripped by the chat-completions transport before the request is sent.
@@ -484,7 +488,6 @@
     message = {
         "role": "tool",
-        "name": name,
         "tool_name": name,
         "content": wrapped,
         "tool_call_id": tool_call_id,
