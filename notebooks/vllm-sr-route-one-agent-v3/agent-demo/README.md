# Pricing Refactor Exercise

This small project calculates a discounted total through two public functions.
Both functions currently repeat the same pricing rule.

The workshop asks Hermes to remove that duplication without changing the
public behavior.

Run the tests with:

```bash
python -m pytest -q
```
